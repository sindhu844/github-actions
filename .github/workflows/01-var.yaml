name: scope-of-variable

on:
  push:
    branches:
      - main
    
jobs:
  name-of-job-var01:
    runs-on: ubuntu-latest
    
    # env:
    #   name1: sindhu >> $GITHUB_ENV
    steps:
    
      - name: print name
        # env:
        #   name: vijay
        run: |
          echo myName = SindhuV >> $GITHUB_ENV
          echo my name is ${myName}
  name-of-job-var02:
    runs-on: windows-latest
    steps:
      - name: printing name from job 1
        run: printing name from job 1 the name is ${myName}


